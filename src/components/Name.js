import React from 'react'
import { Card } from 'react-bootstrap';

const Name = (x) => {
    return (
    <React.Fragment>
        <Card.Title>
        azer
        </Card.Title>
    </React.Fragment>
)
}

export default Name
